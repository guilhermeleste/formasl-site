{
  "name": "formasl-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "generate-assets": "node scripts/generate-social-assets.mjs",
    "build": "npm run generate-assets && astro build",
    "preview": "astro preview",
    "validate": "node scripts/validate-site.mjs"
  },
  "dependencies": {
    "astro": "7.2.10",
    "sharp": "^0.34.4"
  }
}
